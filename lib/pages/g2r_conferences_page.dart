import 'package:flutter/material.dart';

class G2RConferencesPage extends StatelessWidget {
  const G2RConferencesPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder content
    return Scaffold(
      appBar: AppBar(
        title: const Text('Conferences Ranking'),
      ),
      body: const Center(
        child: Text('Guide2Research Conferences Content'),
      ),
    );
  }
}
